# Automated GitHub Actions version updates.
# Creates PRs when pinned actions release new versions.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
